# Golang CI

This example shows how to set up a simple CI workflow for the Golang project

## Setup

Use task definition from task.json

In order to load an image to a kind cluster use this:

```sh
kind load docker-image golang:1.23.1 --name=prescott-test
```
