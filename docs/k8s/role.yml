kind: Role
apiVersion: rbac.authorization.k8s.io/v1
metadata:
  namespace: prescott
  name: prescott-pod
rules:
  - apiGroups: [""]
    resources: ["pods"]
    verbs: ["create", "get", "list", "delete", "watch"]
