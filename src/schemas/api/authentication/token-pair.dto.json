{
  "$id": "api/authentication/token-pair.json",
  "type": "object",
  "properties": {
    "accessToken": { "type": "string" },
    "refreshToken": { "type": "string", "format": "uuid" }
  },
  "required": ["accessToken", "refreshToken"],
  "additionalProperties": false
}
