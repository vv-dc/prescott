{
  "$id": "schema://prescott.dev/domain/log-entry.json",
  "title": "LogEntry",
  "type": "object",
  "properties": {
    "stream": { "type": "string", "enum": ["stderr", "stdout"] },
    "time": { "type": "integer" },
    "content": { "type": "string" }
  },
  "required": ["stream", "time", "content"],
  "additionalProperties": false
}
